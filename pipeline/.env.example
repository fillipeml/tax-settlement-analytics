# --- Extraction pipeline (Python) ----------------------------------------------
# Only needed to run the extraction against the Claude API. The crawler, the dataset build and
# the tests work without it; `pgfn-extract --simulated` produces placeholder records.
ANTHROPIC_API_KEY=
# Model used for extraction. The published dataset was extracted with claude-sonnet-5.
ANTHROPIC_MODEL=claude-sonnet-5
